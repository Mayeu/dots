#!/bin/bash

#HOME='/Users/lainux'
# Ledger path
if [ `uname` == "Darwin" ] ; then
   LDG=`readlink ${LEDGER_FILE}`
else
   LDG=`readlink -f ${LEDGER_FILE}`
fi

cp ${LDG} ${HOME}/.account.ldg.bak
ledger -f ${LDG} -S d print > ${LDG}.clean
mv ${LDG}.clean ${LDG}
perl -pi -e 's/\s+;/ ;/' ${LDG}
